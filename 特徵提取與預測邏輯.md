```mermaid
flowchart TD
    Frame[讀取影片當前幀 Frame] --> MP[送入 MediaPipe Pose 模型]
    
    MP --> Check{是否偵測到骨架?}
    Check -->|否| Skip[跳過預測，保留原畫面]
    Check -->|是| Extract[進入特徵提取階段]
    
    subgraph 特徵提取工程 Feature Engineering
        Extract --> F1[計算靜態特徵: 關節角度、延展度]
        F1 --> F2[計算靜態特徵: 左右對稱性、相對重心]
        F2 --> F3[計算動態特徵: 移動速度與方向向量]
        F3 --> Vector[整合為 30 維特徵向量]
    end
    
    subgraph 模型預測邏輯 Prediction Logic
        Vector --> Scaler[套用 StandardScaler 特徵標準化]
        Scaler --> Model[輸入機器學習模型 fire_dance_model.pkl]
        Model --> Prob[計算 22 種招式之信心度機率]
    end
    
    Prob --> Smoothing[預測結果平滑化處理]
    Smoothing --> Output[於畫面上標註招式名稱與骨架]
